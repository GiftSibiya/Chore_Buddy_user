import 'package:donuts/constants/sizes.dart';

class Intro extends StatelessWidget {
  const Intro({super.key});

  @override
  Widget build(BuildContext context) {
    //----- START POINT -----//
    return 
      child: Scaffold()
    }
}
